# ===================================================================
# 核心构建目录 - CMake
# ===================================================================

build/

# ===================================================================
# IDE 和编辑器文件 - Visual Studio Code
# ===================================================================

.vscode/

# ===================================================================
# 操作系统生成的文件
# ===================================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.tmp
*.temp
Desktop.ini

# Linux
*~

# ===================================================================
# 项目特定文件（A*算法避障项目）
# ===================================================================
# 可执行文件
# 虽然构建目录已被忽略，但以防万一项目在其他地方生成了可执行文件
*.exe
*.out
*.app

# 运行时生成的输出文件
# 程序可能会生成路径规划结果图、性能日志等，这些不应提交到仓库。
# 建议创建一个专门的 output/ 或 results/ 目录来存放它们。
output/
results/
*.log

# (可选) 测试数据
# 如果这些地图文件是项目运行所必需的核心数据，应该保留并提交。
# 但如果它们是临时测试文件、体积巨大或由程序生成，则应忽略。
# src/*.map
